//oef2: functies
function kwadraat(getal: number): number {
  return getal * getal1;
}

function tekstArray(tekst: string, count: number): (number | string)[] {
  return [count, tekst];
}

let separatedTekst = function (
  tekst: string,
  separator: string | undefined = "-"
): string[] {
  return [];
};
